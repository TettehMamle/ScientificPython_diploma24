import functools

def decor(array)
     @functools.wraps(func)
    def wrapper(array):
        if np.array is empty:
            return print("array is empty")
        else:
            return func(array)
    return wrapper